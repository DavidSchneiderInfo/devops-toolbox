version: "3.9"  # optional since v1.27.0
services:
  toolbox:
    build: .
    env_file:
      - .env
    volumes:
      - ./ansible:/devops/ansible
      - ./terraform:/devops/terraform
      - ./packer:/devops/packer
      - ./tmp:/tmp
      - ./bin/entrypoint:/usr/local/bin/entrypoint